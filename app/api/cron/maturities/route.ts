import { processMaturities } from '@/app/actions/wallet'
export async function GET(request: Request) { const secret = process.env.CRON_SECRET; const authorization = request.headers.get('authorization'); if (!secret || authorization !== `Bearer ${secret}`) return Response.json({ error: 'Unauthorized' }, { status: 401 }); try { const processed = await processMaturities(); return Response.json({ processed }) } catch { return Response.json({ error: 'Unable to process maturities' }, { status: 500 }) } }
